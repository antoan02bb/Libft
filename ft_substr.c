#include <stddef.h>

// Allocates (with malloc(3)) and returns a substring
// from the string ’s’.
// The substring begins at index ’start’ and is of
// maximum size ’len’.

// char const *s -> input string
// char 

#include "libft.h"

char *ft_substr(char const *string, unsigned int start, size_t len) {
	// Handle invalid input
	return '0';
}